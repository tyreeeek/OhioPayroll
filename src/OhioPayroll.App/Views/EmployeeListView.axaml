<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:OhioPayroll.App.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="1000" d:DesignHeight="700"
             x:Class="OhioPayroll.App.Views.EmployeeListView"
             x:DataType="vm:EmployeeListViewModel">

    <!-- ============================================================ -->
    <!-- List Panel - Visible when NOT editing                        -->
    <!-- ============================================================ -->
    <Panel>
        <DockPanel IsVisible="{Binding !IsEditing}">

            <!-- Header -->
            <Border DockPanel.Dock="Top" Padding="0,0,0,24">
                <DockPanel>
                    <TextBlock Text="Employees"
                               Classes="page-title"
                               VerticalAlignment="Center"/>
                    <Button Content="+ Add Employee"
                            Command="{Binding AddEmployeeCommand}"
                            HorizontalAlignment="Right"
                            Padding="16,8"
                            Classes="primary"/>
                </DockPanel>
            </Border>

            <!-- Search / Filter Bar -->
            <Border DockPanel.Dock="Top" Padding="0,0,0,16">
                <StackPanel Orientation="Horizontal" Spacing="16">
                    <TextBox Text="{Binding SearchText}"
                             Watermark="Search by name or SSN last 4..."
                             Width="300"/>
                    <CheckBox Content="Show Inactive"
                              IsChecked="{Binding ShowInactive}"
                              VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Empty State -->
            <TextBlock DockPanel.Dock="Top"
                       Text="No employees found. Click '+ Add Employee' to get started."
                       IsVisible="{Binding !Employees.Count}"
                       Foreground="{StaticResource TextMutedBrush}"
                       FontSize="14"
                       HorizontalAlignment="Center"
                       Margin="0,32,0,0"/>

            <!-- Employee DataGrid -->
            <DataGrid ItemsSource="{Binding Employees}"
                      SelectedItem="{Binding SelectedEmployee}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserResizeColumns="True"
                      CanUserSortColumns="True"
                      GridLinesVisibility="Horizontal"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name"
                                        Binding="{Binding FullName}"
                                        Width="*"/>
                    <DataGridTextColumn Header="SSN"
                                        Binding="{Binding MaskedSsn}"
                                        Width="120"/>
                    <DataGridTextColumn Header="Pay Type"
                                        Binding="{Binding PayType}"
                                        Width="90"/>
                    <DataGridTextColumn Header="Rate / Salary"
                                        Binding="{Binding DisplayRate}"
                                        Width="130"/>
                    <DataGridTextColumn Header="Hire Date"
                                        Binding="{Binding HireDate, StringFormat=\{0:MM/dd/yyyy\}}"
                                        Width="110"/>
                    <DataGridTextColumn Header="Status"
                                        Binding="{Binding StatusText}"
                                        Width="80"/>
                    <DataGridTemplateColumn Header="Actions" Width="170">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate x:DataType="vm:EmployeeRowViewModel">
                                <StackPanel Orientation="Horizontal"
                                            Spacing="4"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center">
                                    <Button Content="Edit"
                                            Command="{Binding $parent[DataGrid].((vm:EmployeeListViewModel)DataContext).EditEmployeeCommand}"
                                            CommandParameter="{Binding}"
                                            Padding="8,4"
                                            FontSize="12"
                                            Classes="secondary"/>
                                    <Button Content="Delete"
                                            Command="{Binding $parent[DataGrid].((vm:EmployeeListViewModel)DataContext).RequestDeleteCommand}"
                                            CommandParameter="{Binding}"
                                            Padding="8,4"
                                            FontSize="12"
                                            Classes="danger"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

        </DockPanel>

        <!-- ============================================================ -->
        <!-- Form Panel - Visible when editing                            -->
        <!-- ============================================================ -->
        <ScrollViewer IsVisible="{Binding IsEditing}">
            <DockPanel Margin="0,0,0,32">

                <!-- Form Header -->
                <Border DockPanel.Dock="Top" Padding="0,0,0,24">
                    <TextBlock Text="{Binding FormTitle}"
                               Classes="page-title"/>
                </Border>

                <!-- Validation Error -->
                <Border DockPanel.Dock="Top"
                        IsVisible="{Binding ValidationError, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                        Background="{StaticResource DangerBrush}"
                        CornerRadius="6"
                        Padding="16,12"
                        Margin="0,0,0,16"
                        Opacity="0.15">
                    <TextBlock Text="{Binding ValidationError}"
                               Foreground="{StaticResource DangerBrush}"
                               TextWrapping="Wrap"/>
                </Border>

                <!-- Action Buttons -->
                <Border DockPanel.Dock="Bottom" Padding="0,24,0,0">
                    <StackPanel Orientation="Horizontal"
                                Spacing="12"
                                HorizontalAlignment="Right">
                        <Button Content="Cancel"
                                Command="{Binding CancelEditCommand}"
                                Padding="24,10"
                                Classes="secondary"/>
                        <Button Content="Save Employee"
                                Command="{Binding SaveEmployeeCommand}"
                                Padding="24,10"
                                Classes="primary"/>
                    </StackPanel>
                </Border>

                <!-- Form Body - Two Column Grid -->
                <Grid ColumnDefinitions="*,32,*" RowDefinitions="Auto">

                    <!-- Left Column -->
                    <StackPanel Grid.Column="0" Spacing="16">

                        <!-- Personal Information -->
                        <TextBlock Text="Personal Information"
                                   Classes="section-header"
                                   Margin="0,0,0,8"/>

                        <StackPanel Spacing="4">
                            <TextBlock Text="First Name" Classes="label"/>
                            <TextBox Text="{Binding FirstName}"
                                     Watermark="First name"/>
                        </StackPanel>

                        <StackPanel Spacing="4">
                            <TextBlock Text="Last Name" Classes="label"/>
                            <TextBox Text="{Binding LastName}"
                                     Watermark="Last name"/>
                        </StackPanel>

                        <StackPanel Spacing="4">
                            <TextBlock Text="Social Security Number" Classes="label"/>
                            <TextBox Text="{Binding Ssn}"
                                     Watermark="000-00-0000"
                                     PasswordChar="*"/>
                        </StackPanel>

                        <!-- Pay Configuration -->
                        <TextBlock Text="Pay Configuration"
                                   Classes="section-header"
                                   Margin="0,24,0,8"/>

                        <StackPanel Spacing="4">
                            <TextBlock Text="Pay Type" Classes="label"/>
                            <ComboBox ItemsSource="{Binding PayTypeValues}"
                                      SelectedItem="{Binding PayType}"
                                      HorizontalAlignment="Stretch"/>
                        </StackPanel>

                        <StackPanel Spacing="4"
                                    IsVisible="{Binding IsHourly}">
                            <TextBlock Text="Hourly Rate ($)" Classes="label"/>
                            <NumericUpDown Value="{Binding HourlyRate}"
                                           Minimum="0"
                                           FormatString="F2"
                                           Increment="0.25"/>
                        </StackPanel>

                        <StackPanel Spacing="4"
                                    IsVisible="{Binding IsSalary}">
                            <TextBlock Text="Annual Salary ($)" Classes="label"/>
                            <NumericUpDown Value="{Binding AnnualSalary}"
                                           Minimum="0"
                                           FormatString="F2"
                                           Increment="1000"/>
                        </StackPanel>

                        <!-- Hire Date -->
                        <TextBlock Text="Employment"
                                   Classes="section-header"
                                   Margin="0,24,0,8"/>

                        <StackPanel Spacing="4">
                            <TextBlock Text="Hire Date" Classes="label"/>
                            <DatePicker SelectedDate="{Binding HireDate}"/>
                        </StackPanel>

                    </StackPanel>

                    <!-- Right Column -->
                    <StackPanel Grid.Column="2" Spacing="16">

                        <!-- Tax Information -->
                        <TextBlock Text="Tax Information"
                                   Classes="section-header"
                                   Margin="0,0,0,8"/>

                        <StackPanel Spacing="4">
                            <TextBlock Text="Federal Filing Status" Classes="label"/>
                            <ComboBox ItemsSource="{Binding FilingStatusValues}"
                                      SelectedItem="{Binding FederalFilingStatus}"
                                      HorizontalAlignment="Stretch"/>
                        </StackPanel>

                        <StackPanel Spacing="4">
                            <TextBlock Text="Ohio Filing Status" Classes="label"/>
                            <ComboBox ItemsSource="{Binding FilingStatusValues}"
                                      SelectedItem="{Binding OhioFilingStatus}"
                                      HorizontalAlignment="Stretch"/>
                        </StackPanel>

                        <StackPanel Spacing="4">
                            <TextBlock Text="Federal Allowances" Classes="label"/>
                            <NumericUpDown Value="{Binding FederalAllowances}"
                                           Minimum="0"
                                           Maximum="99"
                                           Increment="1"
                                           FormatString="F0"/>
                        </StackPanel>

                        <StackPanel Spacing="4">
                            <TextBlock Text="Ohio Exemptions" Classes="label"/>
                            <NumericUpDown Value="{Binding OhioExemptions}"
                                           Minimum="0"
                                           Maximum="99"
                                           Increment="1"
                                           FormatString="F0"/>
                        </StackPanel>

                        <!-- Location -->
                        <TextBlock Text="Location"
                                   Classes="section-header"
                                   Margin="0,24,0,8"/>

                        <StackPanel Spacing="4">
                            <TextBlock Text="School District Code" Classes="label"/>
                            <TextBox Text="{Binding SchoolDistrictCode}"
                                     Watermark="e.g. 0401"/>
                        </StackPanel>

                        <StackPanel Spacing="4">
                            <TextBlock Text="Municipality Code" Classes="label"/>
                            <TextBox Text="{Binding MunicipalityCode}"
                                     Watermark="e.g. COLS"/>
                        </StackPanel>

                        <!-- Address -->
                        <TextBlock Text="Address"
                                   Classes="section-header"
                                   Margin="0,24,0,8"/>

                        <StackPanel Spacing="4">
                            <TextBlock Text="Street Address" Classes="label"/>
                            <TextBox Text="{Binding Address}"
                                     Watermark="Street address"/>
                        </StackPanel>

                        <Grid ColumnDefinitions="*,12,80,12,100">
                            <StackPanel Grid.Column="0" Spacing="4">
                                <TextBlock Text="City" Classes="label"/>
                                <TextBox Text="{Binding City}"
                                         Watermark="City"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2" Spacing="4">
                                <TextBlock Text="State" Classes="label"/>
                                <TextBox Text="{Binding State}"
                                         Watermark="OH"/>
                            </StackPanel>
                            <StackPanel Grid.Column="4" Spacing="4">
                                <TextBlock Text="ZIP Code" Classes="label"/>
                                <TextBox Text="{Binding ZipCode}"
                                         Watermark="00000"/>
                            </StackPanel>
                        </Grid>

                    </StackPanel>

                </Grid>

            </DockPanel>
        </ScrollViewer>
    </Panel>

</UserControl>
